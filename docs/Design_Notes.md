#Why you used ArrayList instead of array
1. ArrayList has flexibility to grow its length as items added 
2. ArrayList comes with built in features to do operation on it if required.

#Where you used static members and why
1. To store student and course data, which creates class level data instead of object level. 
2. To created ids of each entity to maintain single sequence at class level.

#Where you used inheritance and what you gained from it
1. Reused person object to avoid repeated methods and variable creation. Simply reused existing resource.